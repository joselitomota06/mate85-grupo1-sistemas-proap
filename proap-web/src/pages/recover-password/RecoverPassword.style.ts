import styled from '@emotion/styled';
import { Box, Paper } from '@mui/material';

export const RecoverPasswordBox = styled(Box)`
  height: 100%;
`;

export const RecoverPasswordPaperContainer = styled(Paper)`
  padding: 2rem 1rem;
  max-width: 400px;
  margin: auto;
`;
